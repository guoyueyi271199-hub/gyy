# 个人简历原型 —— 项目约定

单文件 HTML 原型：`index.html`（深色主题个人简历，约 490 行，无需构建）。

## 结构约定
- 所有可调间距收敛为 `:root` 下 `--sp-*` 的 CSS 变量（`--sp-section-v` 区块垂直间距、`--sp-container-h` 容器左右内边距、`--sp-card-v` 卡片内边距、`--sp-card-gap` 卡片间距、`--sp-timeline-mb` 时间线条目间距）。
- 页面带实时调参面板：`#sp-toggle`（右侧竖条）滑出 `#sp-panel`，每个滑块通过 `data-k` 绑定对应 `--sp-*` 变量；`#sp-reset` 重置默认；数值存 `localStorage`（键名唯一，勿改）。

## 修改规则
1. 改**布局节奏/间距**：优先改 `--sp-*` 变量的默认值，不要硬编码具体 px。间距步进：节奏类 `step=4`，gutter/卡片内边距 `step=2`。
2. 改**主题/颜色**：改 `:root` 里的 `--bg/--card/--ink/--muted/--brand` 等变量，保持深色风格与 indigo 主强调 (`#5E6AD2`)。
3. 改**结构/文案**：直接在对应 section 内编辑，保持中文文案与既有语义化标签。
4. **不要把调参面板删掉**（它是开发辅助，定稿前保留；只在用户明确要求"定稿/发布"时才移除并回写默认值）。
5. 保持单文件、无外部依赖、可 `file://` 直接打开（`localStorage` 访问必须包在 `try/catch`）。
6. 编辑后保持代码风格整洁，`--ease` 缓动与 `.aurora` 光斑动效等既有视觉语言不要破坏。

## 验证
改完在浏览器打开 `index.html` 确认：无 JS 报错、调参面板能弹开、间距/主题实时生效、刷新后值保留。
